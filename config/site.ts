export type SiteConfig = typeof siteConfig;

export const siteConfig = {
	name: "Pull Espresso",
	description: "Make better espresso shots.",
	navItems: [
		{
			label: "Home",
			href: "/",
		},
	],
	navMenuItems: [],
	links: {
		site: 'https://pull-espresso.vercel.app'
	},
};
